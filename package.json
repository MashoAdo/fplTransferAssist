{
  "name": "transferassist",
  "version": "1.0.0",
  "description": "Fpl Transfer assist",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run": "nodemon run",
    "start": "node server.js",
    "build": "cd client && npm run build",
    "install": "cd client && npm install",
    "heroku-postbuild": "npm install --prefix client && npm run build --prefix client"
  },
  "author": "Masho Ado Nache",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.2",
    "nodemon": "^2.0.15",
    "request": "^2.88.2"
  }
}
